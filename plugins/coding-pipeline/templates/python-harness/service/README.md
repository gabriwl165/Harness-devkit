# Python service

<!-- Harness bootstrap metadata: generator=init-python-harness profile=service -->

This framework-neutral service policy scaffold uses the existing project marker. Add application code, an HTTP server, deployment configuration, and framework/runtime choices in later phases.
