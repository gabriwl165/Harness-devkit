# Agent controls

This project is governed by `python-harness-policy.toml`. Review and falsification are required.
The owner placeholder must be replaced by the consumer before CI. Local host controls are
integration points only; these documents do not enforce runtime permissions.
