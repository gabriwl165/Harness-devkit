# Python library

<!-- Harness bootstrap metadata: generator=init-python-harness profile=library -->

This framework-neutral library scaffold is ready for package code and tests.
Application entrypoints, servers, deployment manifests, and framework/runtime choices are intentionally deferred.
