# Quality policy

<!-- Harness bootstrap metadata: generator=init-python-harness profile=library -->

Use the repository Python sensors: `ruff format --check .`, `ruff check .`, `mypy .`, `pytest --cov --cov-fail-under=85`, and `pip-audit`.
