# MCP governance

No MCP server is configured or enabled by default. The allowlist is empty. Local host controls
are documented integration points only and are not generated or enforced by this project.
